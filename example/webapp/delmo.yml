---
suite:
  name: Webapp
  system: docker-compose.yml
  task_service: tests

tests:
- name: webapp_fails
  spec:
  - wait: [webapp_is_available]
  - assert: [webapp_is_available]
  - stop: [webapp]
  - fail: [webapp_is_available]

tasks:
- name: webapp_is_available
  command: /tasks/webapp_is_available.sh
