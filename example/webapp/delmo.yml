---
system:
  name: Webapp
  file: docker-compose.yml

# tasks:
# - name: webapp_is_available
#   image: webapp
#   run:
#     path: echo
#     args: [hello, world]

tests:
- name: webapp_fails
  spec:
  - {stop: [webapp]}
  - {assert: [webapp_is_available]}
  - {restart: [webapp]}
  - {assert: [webapp_is_available]}
