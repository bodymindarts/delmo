---
suite:
  name: Webapp
  system: docker-compose.yml
  task_service: tests

tests:
- name: webapp_fails
  spec:
  # - ready: [webapp_is_available]
  - stop: [webapp]
    # failing: [webapp_is_available]
  - start: [webapp]
  - assert: [webapp_is_available]

tasks:
- name: webapp_is_available
  command: /tasks/webapp_is_available.sh
