---
suite:
  name: Webapp
  file: docker-compose.yml

tasks:
- name: webapp_is_available
  image: webapp
  run:
    path: scripts/webapp_is_available.sh

tests:
- name: webapp_fails
  spec:
  # - ready: [webapp_is_available]
  - stop: [webapp]
    # failing: [webapp_is_available]
  - start: [webapp]
  - assert: [webapp_is_available]
